# iOS App

The iOS app will use SwiftUI and consume the shared KMP module.

## Prerequisites
- Xcode 16+
- Run `./gradlew :shared:assembleXCFramework` to build the shared framework

## Status
iOS UI is a placeholder. The shared business logic (data, domain, DI) is fully functional and can be consumed from Swift.
